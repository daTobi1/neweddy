[project]
name = "eddy-ng"
requires-python = ">=3.8"

[tool.pytest.ini_options]
testpaths = ["tests"]
# Don't recurse into Klipper plugin directories
norecursedirs = ["probe_eddy_ng", "__pycache__", ".git"]
